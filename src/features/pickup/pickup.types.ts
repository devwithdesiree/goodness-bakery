export interface PickupFormState {
  name: string;
  email: string;
  phone: string;
  flavor: string;
  boxSize: string;
  quantity: number;
  pickupDay: string;
  notes: string;
  smsConsent: boolean;
}
